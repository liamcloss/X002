# Speculation Edge configuration template
# Copy this file to `.env` and populate it with real credentials before running any commands.

# Trading212 API credentials (used for account/pos data only).
T212_API_KEY=
T212_API_SECRET=

# Telegram credentials used by both the core CLI alerts and the ops bot.
TELEGRAM_BOT_TOKEN=
# Primary chat for alerts (closed group or channel).
TELEGRAM_CHAT_ID=
# Optional chat for paper-trade-only alerts.
TELEGRAM_PAPER_CHAT_ID=
# Comma-separated user IDs allowed to control the ops bot.
TELEGRAM_ADMIN_USERS=                 # e.g. 6086802750,123456789

# Ops Telegram bot controls
# Use a specific Python interpreter if you run inside a virtualenv.
OPS_PYTHON=.venv/Scripts/python.exe
# Defines how much command output the bot relays (full|errors|summary|none).
OPS_OUTPUT_MODE=full
# Commands that write artifacts but shouldn't reply except for status/errors.
OPS_SILENT_COMMANDS=market_data,universe  # comma separated commands that stay quiet
# Commands that never receive a Telegram reply on success.
OPS_NO_REPLY_COMMANDS=pretrade

# Scheduler hooks (leave disabled by clearing OPS_SCHEDULE_ENABLED or chat ID).
OPS_SCHEDULE_ENABLED=1  # set to 0 to disable scheduler
# Where scheduled job outputs go.
OPS_SCHEDULE_CHAT_ID=   # chat id for scheduled job outputs (e.g. 5181900798)
# 1=UTC timestamps, 0=local time.
OPS_SCHEDULE_USE_UTC=0
# Daily market-data refresh after midnight.
OPS_SCHEDULE_MARKET_DATA=daily 00:05   # format: daily HH:MM or mon/sun HH:MM
# Morning scan.
OPS_SCHEDULE_SCAN=daily 06:30          # use daily or weekday/weekend tokens
# Weekday pretrade guard.
OPS_SCHEDULE_PRETRADE=weekday 08:00
# Pre-scan Mooner job.
OPS_SCHEDULE_MOONER=daily 05:45         # Mooner runs before scan for fresh context
# Weekly YOLO penny lottery (optional).
OPS_SCHEDULE_YOLO=weekly mon 04:00

# Optional AI key for news scout enrichment (only used if news_scout.llm_enabled=true)
OPENAI_API_KEY=
